<?php
// $Id$
/**
 * @file fail.module
 * Designed to create failures we can debug.
 */


function fail_init() {
  $node = node_load(arg('asfd'));
  //if (arg(0) == 'node') {
    drupal_set_m1essage('this is a test of an argument %argument', array('%argument' => 'no contest'));
  //}
}
